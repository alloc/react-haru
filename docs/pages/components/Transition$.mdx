---
title: Transition component
---

Transition wraps useTransition

Tree-shaked if you don't import it

The `items` prop

The `keys` prop

The render prop

Learn more on the [`useTransition`](/hooks/useTransition) page.
