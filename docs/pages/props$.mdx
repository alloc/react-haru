---
title: Spring props
---

This page describes the props which are supported by the `SpringValue` class. Consequentially, these props can be used anywhere that accepts what's called a "spring update" (or "update" for short).

# Common props

# Default props
